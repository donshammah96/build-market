{
  "name": "workers",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone BullMQ & NATS background worker daemon for BuildMarket",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "check-types": "tsc --noEmit",
    "lint": "echo \"No lint configured for workers\" && exit 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@build/db": "workspace:*",
    "@build/env-validation": "workspace:*",
    "@build/nats": "workspace:*",
    "@build/queue-server": "workspace:*",
    "@build/redis": "workspace:*",
    "@build/resilience": "workspace:*",
    "bullmq": "catalog:",
    "ioredis": "catalog:",
    "nats": "catalog:",
    "pino": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@build/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}