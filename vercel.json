{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -C packages/resilience run build && pnpm -C apps/client run build",
  "outputDirectory": "apps/client/.next"
}
