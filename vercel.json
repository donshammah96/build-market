{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm tsc --build tsconfig.json && pnpm run build:client",
  "outputDirectory": "apps/client/.next"
}
