{
  "name": "@build/messaging-server",
  "version": "1.0.0",
  "description": "Server-side messaging domain facade",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "lint": "echo \"No lint configured\" && exit 0",
    "check-types": "echo \"Facade package typecheck skipped\" && exit 0",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "@build/typescript-config": "workspace:*",
    "@types/node": "^24.8.1",
    "typescript": "^5.9.3"
  }
}
