# nats-server.conf — Render Free Tier Web Service
# Uses WebSocket on port 10000 for Render public HTTPS/WSS routing

websocket {
  port: 10000
  no_tls: true
}

http_port: 8222

jetstream {
  store_dir: "/tmp/jetstream"
  max_mem: 256M
  max_file: 1G
}

authorization {
  token: "$NATS_TOKEN"
}
